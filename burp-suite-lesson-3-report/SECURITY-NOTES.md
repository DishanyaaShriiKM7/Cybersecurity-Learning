# Screenshot handling

Only screenshots that did not visibly expose authentication/session-cookie values were copied into `assets/`.

Several uploaded screenshots contained session cookies or other live-site cookie values. Those were intentionally excluded from this GitHub-ready package.

If you add more screenshots later, redact:
- `Cookie:` headers
- session IDs
- bearer tokens
- API keys
- email addresses / account identifiers if private
